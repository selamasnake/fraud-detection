# Tests

This folder contains unit tests for the project using **pytest**.

- **test_data_processing.py**: Tests for `DataCleaner` and `EDAProcessor` in `src/data_processing.py`.

**How to run tests:**
```bash
pytest tests